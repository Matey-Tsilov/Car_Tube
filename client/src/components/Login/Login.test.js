import Login from './Login'
    import {render, screen } from "@testing-library/react";

test('Should render flawlessly', () => {
    // const text = "Login"
    
    // render(<Login />);

    // expect(screen.getByText(text)).toBeInTheDocument();
});